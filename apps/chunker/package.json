{
  "name": "@showcasehost/chunker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && node --test dist/index.test.js"
  },
  "dependencies": {
    "code-chunk": "0.1.14",
    "linguist-languages": "9.4.0",
    "tree-sitter-wasm": "1.1.6",
    "web-tree-sitter": "0.26.3"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.3"
  }
}
